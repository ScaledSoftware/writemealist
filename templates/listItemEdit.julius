function setInitialFocus() {
    var target = document.getElementById(#{toJSON aDomId});
    target.focus(); 
    target.setSelectionRange(target.value.length, target.value.length);
}

onload = function() {
   setInitialFocus();
}
